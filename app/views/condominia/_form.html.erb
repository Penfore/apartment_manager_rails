<%= form_with(model: condominium) do |form| %>
  <% if condominium.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(condominium.errors.count, "error") %> prohibited this condominium from being saved:</h2>

      <ul>
        <% condominium.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
