<%= form_with(model: emergency) do |form| %>
  <% if emergency.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(emergency.errors.count, "error") %> prohibited this emergency from being saved:</h2>

      <ul>
        <% emergency.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tenant_id, style: "display: block" %>
    <%= form.collection_select :tenant_id, @tenants, :id, :name %>
  </div>

  <div>
    <%= form.label :owner_id, style: "display: block" %>
    <%= form.collection_select :owner_id, @owners, :id, :name %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :parentage, style: "display: block" %>
    <%= form.text_field :parentage %>
  </div>

  <div>
    <%= form.label :contact, style: "display: block" %>
    <%= form.text_field :contact %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
