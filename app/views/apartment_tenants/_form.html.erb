<%= form_with(model: apartment_tenant) do |form| %>
  <% if apartment_tenant.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(apartment_tenant.errors.count, "error") %> prohibited this apartment_tenant from being saved:</h2>

      <ul>
        <% apartment_tenant.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :apartment_id, style: "display: block" %>
    <%= form.collection_select :apartment_id, @apartments, :id, :number %>
  </div>

  <div>
    <%= form.label :tenant_id, style: "display: block" %>
    <%= form.collection_select :tenant_id, @tenants, :id, :name %>
  </div>

  <div>
    <%= form.label :current, style: "display: block" %>
    <%= form.check_box :current %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
